# apps/desk-web — LEGACY / BACKUP

**Este frontend NO se usa en el flujo actual del proyecto.**

- El SaaS se sirve desde **Laravel + Inertia + React** en `resources/js` (puerto **8000**).
- `composer run dev` y `npm run dev` del **root** levantan el frontend Inertia.
- Esta carpeta se mantiene solo como **respaldo/referencia** del antiguo SPA (Vite + React independiente).

Para correr el proyecto: desde la raíz del repo, ejecutar `composer run dev`. No uses esta carpeta salvo que quieras recuperar código del SPA antiguo.
